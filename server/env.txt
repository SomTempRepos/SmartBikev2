# Copy this file to .env and update the values

# ┌────────────────────────┐
# │ Server Configuration   │
# └────────────────────────┘
PORT=3001
HOST=0.0.0.0   # 👈 Allows access from any network interface (LAN included)
NODE_ENV=development

# ┌────────────────────────┐
# │ CORS Configuration     │
# └────────────────────────┘
# Allow frontend running on LAN (update to your actual IP)
CORS_ORIGIN=http://10.141.251.240:5173,http://localhost:5173

# ┌────────────────────────┐
# │ JWT Configuration      │
# └────────────────────────┘
JWT_SECRET=supersecretkey_for_dev_only

# ┌────────────────────────┐
# │ MQTT Configuration     │
# └────────────────────────┘
MQTT_BROKER=mqtt://broker.hivemq.com
MQTT_TOPIC_PREFIX=SmartCycle

# ┌────────────────────────┐
# │ File & Data Storage    │
# └────────────────────────┘
DATA_DIR=./data
DAILY_DIR=./data/daily

USERS_FILE=./data/users.json
RANKS_FILE=./data/ranks.json
GUARDIANS_FILE=./data/guardians.json
BIKES_FILE=./data/bikes.json

# ┌────────────────────────┐
# │ Socket.IO Configuration│
# └────────────────────────┘
SOCKET_PING_TIMEOUT=30000
SOCKET_PING_INTERVAL=5000


# Environment
NODE_ENV=development